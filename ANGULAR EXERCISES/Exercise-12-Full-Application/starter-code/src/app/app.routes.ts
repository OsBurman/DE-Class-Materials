import { Routes } from '@angular/router';

// TODO: Define all routes
//   '' → DashboardComponent (or redirect)
//   'tasks' → TaskListComponent
//   'tasks/new' → TaskFormComponent  ← must come BEFORE 'tasks/:id'
//   'tasks/:id' → TaskDetailComponent
//   'tasks/:id/edit' → TaskFormComponent
//   '**' → NotFoundComponent
export const routes: Routes = [];
