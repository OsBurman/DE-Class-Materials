package com.exercise.employeemanager.exception;

// TODO 3: Make this class extend RuntimeException.
//         RuntimeException is unchecked — callers don't need to declare or catch it.
//
// TODO 4: Add a constructor: ResourceNotFoundException(String message)
//         that calls super(message)
//
// TODO 5: Add a convenience constructor: ResourceNotFoundException(String resource, Long id)
//         that calls this(resource + " not found with id: " + id)
//         Usage: throw new ResourceNotFoundException("Employee", 99L)
//         → message: "Employee not found with id: 99"
public class ResourceNotFoundException {

    // TODO 3: extend RuntimeException

    // TODO 4 & 5: your constructors here
}
