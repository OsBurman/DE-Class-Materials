import { ApolloClient, InMemoryCache } from "@apollo/client";

// TODO 1: Create and export an ApolloClient instance.
//         Set the `uri` to "http://localhost:4000/graphql"
//         and `cache` to a new InMemoryCache().
//
// Example shape:
//   export const client = new ApolloClient({ uri: "...", cache: ... });

export const client = null as any; // replace this line with your implementation
