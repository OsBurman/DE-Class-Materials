import { Navigate } from 'react-router-dom'

// TODO: Implement ProtectedRoute({ user, children }).
//   If user is null (not logged in), redirect to /login using <Navigate to="/login" replace />.
//   If user exists, render children.

function ProtectedRoute({ user, children }) {
  // TODO: implement
  return children
}

export default ProtectedRoute
