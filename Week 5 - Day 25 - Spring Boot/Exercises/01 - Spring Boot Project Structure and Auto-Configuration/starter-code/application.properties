# Enables the full auto-configuration CONDITIONS EVALUATION REPORT on startup.
# Look for these sections in the console output:
#   - Positive matches: auto-configurations that were applied
#   - Negative matches: auto-configurations that were skipped (and why)

# TODO: Add the property that enables debug-level auto-configuration reporting.
#       Property name: debug
#       Value: true

# --- Reflection Question ---
# After starting the app, find "DispatcherServletAutoConfiguration" in the report.
# Q: What condition caused it to match (be applied)?
# A: (write your answer here)
