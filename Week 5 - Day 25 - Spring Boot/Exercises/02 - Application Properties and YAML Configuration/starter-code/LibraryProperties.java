package com.library.config;

// TODO: Import @ConfigurationProperties from org.springframework.boot.context.properties

/**
 * Typed configuration holder for all properties prefixed with "library".
 *
 * Spring Boot will bind:
 *   library.max-loan-days     → maxLoanDays
 *   library.welcome-message   → welcomeMessage
 *
 * TODO:
 *   1. Add @ConfigurationProperties(prefix = "library") to this class
 *   2. Declare a private int field named maxLoanDays
 *   3. Declare a private String field named welcomeMessage
 *   4. Generate (or write) getters and setters for both fields
 *      (Spring needs setters to inject the values)
 */
public class LibraryProperties {

    // TODO: declare fields here

    // TODO: add getters and setters for maxLoanDays

    // TODO: add getters and setters for welcomeMessage
}
