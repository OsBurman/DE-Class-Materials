# Spring Boot Packaging Commands

Fill in each command below.

## 1. Build the executable fat JAR (skip tests)
```
# TODO: Write the Maven command that packages the project into a fat JAR and skips tests.
#       The output file will be at target/library-service-0.0.1-SNAPSHOT.jar
```

## 2. Run the fat JAR from the command line
```
# TODO: Write the java -jar command to run the packaged JAR
```

## 3. Run with a different profile (prod) from the command line
```
# TODO: Write the java -jar command with the argument that sets spring.profiles.active=prod
```

## 4. Run directly with Maven (without packaging first)
```
# TODO: Write the mvn command that uses the Spring Boot Maven plugin to run the app directly
```

## 5. What is the difference between a "thin JAR" and a "fat (uber) JAR"?
> TODO: Answer in 1–2 sentences.
