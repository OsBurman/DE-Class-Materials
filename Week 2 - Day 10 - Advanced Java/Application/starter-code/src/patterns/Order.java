import java.util.List;

/**
 * Represents a coffee order built using the Builder pattern.
 *
 * TODO Task 3:
 * 1. Add fields: drink (Drink), size (String), extras (List<String>),
 * customerName (String), totalPrice (double)
 * 2. Private constructor (only Builder can create Order)
 * 3. Create static inner class Builder with:
 * - withDrink(Drink d) → returns Builder
 * - withSize(String s) → returns Builder
 * - addExtra(String extra) → returns Builder
 * - forCustomer(String name) → returns Builder
 * - build() → calculates totalPrice and returns new Order
 * 4. Getters for all fields
 * 5. Override toString() for receipt display
 */
public class Order {

    // TODO: private fields

    // TODO: private constructor

    // TODO: static inner class Builder

    // TODO: getters + toString
}
