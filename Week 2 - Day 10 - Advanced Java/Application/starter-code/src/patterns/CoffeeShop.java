/**
 * The CoffeeShop class — implemented as a SINGLETON.
 *
 * TODO Task 1:
 * 1. Add a private constructor
 * 2. Add a private static CoffeeShop instance field
 * 3. Implement getInstance() — create the instance only if it's null (lazy
 * initialization)
 * 4. Add private List<OrderObserver> observers
 * 5. Implement addObserver(), notifyObservers(Order order), placeOrder(Order
 * order)
 */
public class CoffeeShop {

    // TODO: private static CoffeeShop instance;

    // TODO: private constructor

    // TODO: public static CoffeeShop getInstance()

    // TODO: private List<OrderObserver> observers = new ArrayList<>();

    // TODO: addObserver, notifyObservers, placeOrder

    // TODO: private PricingStrategy pricingStrategy (default: RegularPricing)
    // TODO: setPricingStrategy(PricingStrategy s)
}
