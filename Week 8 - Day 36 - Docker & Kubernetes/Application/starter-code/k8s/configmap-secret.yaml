# Kubernetes — ConfigMap + Secret
# TODO Task 4: Complete the ConfigMap and Secret

apiVersion: v1
kind: ConfigMap
metadata:
  name: academy-config
  namespace: academy
data:
  # TODO Task 4a: Add non-sensitive config values
  SPRING_PROFILES_ACTIVE: "prod"
  SERVER_PORT: "8080"
  # TODO: Add database URL here (not the password!)

---

apiVersion: v1
kind: Secret
metadata:
  name: academy-secret
  namespace: academy
type: Opaque
data:
  # TODO Task 4b: Base64-encode values: echo -n 'value' | base64
  # DB_PASSWORD: <base64-encoded-password>
  # DB_USERNAME: <base64-encoded-username>
  DB_PASSWORD: c2VjcmV0       # "secret"
  DB_USERNAME: YWNhZGVteQ==   # "academy"
