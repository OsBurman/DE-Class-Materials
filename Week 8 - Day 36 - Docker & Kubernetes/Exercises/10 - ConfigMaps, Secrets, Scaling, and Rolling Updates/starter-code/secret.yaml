# Exercise 10 — Secret Manifest Starter
# Complete every TODO.
# IMPORTANT: Values in the data block must be base64-encoded.
# echo -n 'app'         | base64  →  YXBw
# echo -n 'supersecret' | base64  →  c3VwZXJzZWNyZXQ=

# TODO: Set apiVersion to v1
apiVersion: # TODO
# TODO: Set kind to Secret
kind: # TODO

metadata:
  # TODO: Name the Secret spring-secret
  name: # TODO
  namespace: default
  # TODO: Add type Opaque
  # type is a sibling of metadata, not inside it — see below

# TODO: Set type to Opaque (generic key-value secret)
type: # TODO

data:
  # TODO: Add DB_USERNAME with base64 value for "app"
  # DB_USERNAME: TODO
  # TODO: Add DB_PASSWORD with base64 value for "supersecret"
  # DB_PASSWORD: TODO
