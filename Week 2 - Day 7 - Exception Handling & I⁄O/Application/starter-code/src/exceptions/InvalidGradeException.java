package exceptions;

/**
 * Thrown when a student's GPA is outside the valid range (0.0 - 4.0).
 * This is a CHECKED exception — callers must declare or handle it.
 *
 * TODO Task 1: Extend Exception.
 * Add a constructor that accepts a String message and passes it to super().
 */
public class InvalidGradeException extends Exception {

    // TODO: public InvalidGradeException(String message) { super(message); }
}
