// src/utils.js

// TODO Task 8: Map cache and Set for search history
// export const weatherCache = new Map();
// export const searchHistory = new Set();


// TODO Task 6: mergeWeatherData — uses rest and spread
// export function mergeWeatherData(...dataSources) {
//   return dataSources.reduce((acc, source) => ({ ...acc, ...source }), {});
// }


// TODO Task 7: formatWeatherSummary — uses destructuring with default params
// export function formatWeatherSummary({ city, temp, description } = {}) {
//   if (!city) return 'No weather data';
//   return `${city}: ${temp}°C — ${description}`;
// }
