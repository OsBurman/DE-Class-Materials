// stringUtils.js — starter code

// TODO: Requirement 4 — create and NAMED EXPORT two functions:
//       - capitalize(str): returns str with its first character uppercased (rest unchanged)
//         e.g. capitalize("hello") → "Hello"
//       - reverseString(str): returns str reversed character-by-character
//         e.g. reverseString("module") → "eludom"
