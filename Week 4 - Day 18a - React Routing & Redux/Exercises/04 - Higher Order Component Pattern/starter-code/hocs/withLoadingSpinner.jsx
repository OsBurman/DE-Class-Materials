// src/hocs/withLoadingSpinner.jsx  (starter)
import React from 'react';

// TODO 1: Define and export a function withLoadingSpinner(WrappedComponent).
//         Inside it, define and return an Enhanced component.
//         - Destructure `isLoading` and `...rest` from props.
//         - If isLoading is true, return <p>Loading…</p>
//         - Otherwise, return <WrappedComponent {...rest} />
//         Set Enhanced.displayName for debugging.

function withLoadingSpinner(WrappedComponent) {
  // TODO: implement
}

export default withLoadingSpinner;
