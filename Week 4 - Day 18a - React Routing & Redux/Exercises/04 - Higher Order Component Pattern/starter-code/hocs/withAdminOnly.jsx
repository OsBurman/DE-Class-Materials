// src/hocs/withAdminOnly.jsx  (starter)
import React from 'react';

// TODO 2: Define and export a function withAdminOnly(WrappedComponent).
//         Inside it, define and return an Enhanced component.
//         - Destructure `isAdmin` and `...rest` from props.
//         - If isAdmin is false, return <p>Access Denied. Admins only.</p>
//         - Otherwise, return <WrappedComponent {...rest} />
//         Set Enhanced.displayName for debugging.

function withAdminOnly(WrappedComponent) {
  // TODO: implement
}

export default withAdminOnly;
