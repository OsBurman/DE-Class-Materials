import java.util.*;

/**
 * Generic Utility Methods — STARTER CODE
 *
 * TODO 1: swap — generic swap of two array elements
 *   public static <T> void swap(T[] arr, int i, int j)
 *
 * TODO 2: findMax — return the maximum element from a list
 *   public static <T extends Comparable<T>> T findMax(List<T> list)
 *   Hint: use list.get() and compareTo()
 *
 * TODO 3: sumList — sum a list of any Number subtype (wildcard)
 *   public static double sumList(List<? extends Number> list)
 *   Hint: for (Number n : list) total += n.doubleValue();
 *
 * TODO 4: repeat — create a List<T> containing element repeated `times` times
 *   public static <T> List<T> repeat(T element, int times)
 */
public class Utils {

    // TODO 1
    // public static <T> void swap(T[] arr, int i, int j) { ... }

    // TODO 2
    // public static <T extends Comparable<T>> T findMax(List<T> list) { ... }

    // TODO 3
    // public static double sumList(List<? extends Number> list) { ... }

    // TODO 4
    // public static <T> List<T> repeat(T element, int times) { ... }
}
