import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class AuthService {
  // TODO 1 – Add a public `isLoggedIn: boolean` property, initially false

  // TODO 2 – Add a login() method that sets isLoggedIn to true
  // TODO 2 – Add a logout() method that sets isLoggedIn to false
}
