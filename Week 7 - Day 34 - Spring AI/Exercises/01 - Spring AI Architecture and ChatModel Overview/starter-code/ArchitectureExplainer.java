package com.springai;

public class ArchitectureExplainer {

    public static void main(String[] args) {
        printArchitecture();
    }

    /**
     * TODO 5: Print a structured overview of Spring AI's architecture.
     * Cover these five layers:
     *   Layer 1: Model Abstraction  — ChatModel, EmbeddingModel
     *   Layer 2: Prompt Building    — PromptTemplate, Message types
     *   Layer 3: Data & Retrieval   — VectorStore, DocumentReader
     *   Layer 4: Model Providers    — OpenAI, Azure, Anthropic, Ollama, Mistral
     *   Layer 5: Spring Integration — auto-config, @AiService
     *
     * Start with: System.out.println("=== Spring AI Architecture Overview ===");
     * Use indented println calls for each item under each layer heading.
     */
    static void printArchitecture() {
        // TODO 5: Implement the five-layer architecture overview
    }
}
