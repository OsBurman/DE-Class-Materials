spring:
  application:
    name: study-assistant
  ai:
    openai:
      # TODO: Set your OpenAI API key here or via OPENAI_API_KEY environment variable
      api-key: ${OPENAI_API_KEY:your-api-key-here}
      chat:
        options:
          model: gpt-3.5-turbo
          temperature: 0.7
          max-tokens: 1000

server:
  port: 8080
