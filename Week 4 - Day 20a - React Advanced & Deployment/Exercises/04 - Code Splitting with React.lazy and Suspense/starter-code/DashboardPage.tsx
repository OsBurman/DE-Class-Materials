// DashboardPage.tsx — a lazily loaded page component (has a simulated delay)
export default function DashboardPage() {
  return (
    <div>
      <h2>Dashboard Overview</h2>
      <p>Your key metrics and activity summary are displayed here.</p>
    </div>
  );
}
