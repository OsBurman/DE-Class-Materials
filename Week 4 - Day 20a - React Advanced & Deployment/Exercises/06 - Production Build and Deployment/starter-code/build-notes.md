# Build Notes

Fill in the blanks based on what you discover running `npm run build`.

1. **Default output folder:**
   Vite outputs the production build to the `___` folder.

2. **Purpose of hashed filenames (e.g. `index-Bx3k9aQ2.js`):**
   The hash in the filename is used for ___.
   This means browsers can ___ the file indefinitely and only re-download when ___.

3. **Why add `dist/` to `.gitignore`?**
   Because ___.

4. **What is the difference between `sourcemap: true` and `sourcemap: 'hidden'`?**
   `true` → ___
   `'hidden'` → ___
