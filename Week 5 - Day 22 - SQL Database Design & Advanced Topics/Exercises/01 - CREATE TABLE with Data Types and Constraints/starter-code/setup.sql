-- =============================================================
-- Exercise 01: CREATE TABLE with Data Types and Constraints
-- File: starter-code/setup.sql
-- Description: Reference context only — no setup needed for this
--              exercise. Students create all tables from scratch.
-- =============================================================

-- Run this file first to ensure a clean slate.
-- WARNING: Drops tables if they already exist (in dependency order).

DROP TABLE IF EXISTS members;
DROP TABLE IF EXISTS books;
DROP TABLE IF EXISTS genres;
DROP TABLE IF EXISTS authors;
