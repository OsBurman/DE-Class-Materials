package com.library.exception;

// TODO: Define a record with three components: int status, String message, String timestamp
public record ErrorResponse() {
    // Replace the empty record with: (int status, String message, String timestamp)
}
