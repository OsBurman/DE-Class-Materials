import { Component } from '@angular/core';

// TODO 1: Add a one-sentence comment here explaining what @Component does.

@Component({
  // TODO 2: Set the selector to 'app-root'.
  // The selector is the custom HTML tag Angular replaces with this component's template.
  selector: '',

  // TODO 3: Set the templateUrl to './app.component.html'.
  // templateUrl points to the external HTML file that defines this component's view.
  templateUrl: '',

  styleUrls: ['./app.component.css']
})
export class AppComponent {
  // TODO 4: Add a title property with the value 'My Angular App'.
  // Properties declared here are accessible in the template via {{ title }}.
}
