// TODO: export a `config` object with the following typed fields:
//   apiBaseUrl:      read from process.env.REACT_APP_API_BASE_URL, default to ''
//   featureDarkMode: read from process.env.REACT_APP_FEATURE_DARK_MODE,
//                    true only when the string value === 'true'

export const config = {
  apiBaseUrl:      '', // TODO: replace with process.env.REACT_APP_API_BASE_URL ?? ''
  featureDarkMode: false, // TODO: replace with the correct expression
};
